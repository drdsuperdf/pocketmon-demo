// import type { NextConfig } from "next";

import { i18n } from './next-i18next.config';


// const nextConfig: NextConfig = {
// set multitple object key in config then un-specific type of the nextConfig
const nextConfig = {
  /* config options here */
  i18n: i18n,
};

export default nextConfig;